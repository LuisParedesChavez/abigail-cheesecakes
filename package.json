{
  "name": "abigail-cheesecakes",
  "version": "0.0.0",
  "private": true,
  "description": "Monorepo for Abigail's Cheesecakes — Next.js storefront + admin, Supabase backend, Turborepo.",
  "packageManager": "pnpm@11.13.1",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.9.5",
    "turbo": "^2.10.5"
  }
}
